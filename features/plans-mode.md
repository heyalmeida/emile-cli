# Feature: Plans mode

| Field | Value |
|-------|-------|
| **Status** | `active` |
| **Delivery date** | 2026-08-30 |
| **Source spec** | `specs/2026-08-30-plans-compression-resilience` |
| **PRD RFs served** | RF-10 |
| **Owner/Area** | Agent Loop / Planning |

## Description

Plans mode lets a developer inspect and approve the direction of a task before
the model starts executing it. The approval is requested before the first
model stream, so a rejected plan makes no model or tool call.

## How It Works

The CLI enables the mode with `-p` or the prompt toggle. The agent presents a
bounded task preview for approval, then follows the plan-aware system prompt
and renders progress from `task.md` when present.

## Technical Details

| Item | Detail |
|------|---------|
| **CLI flags** | `-p, --plans` |
| **Slash commands** | Prompt `Tab` toggle; `/help` documents the mode |
| **Tools** | Existing built-in and MCP tools after approval |
| **Configuration** | `implementation_plan.md` and `task.md` are read from the workspace |
| **Applicable security gates** | Approval precedes all model/tool activity; writes retain normal safe-mode gates |

## Where It Lives in the Code

| Layer | Main paths |
|--------|---------------------|
| Approval and status | `src/plans.js` |
| Agent integration | `src/agent/agent.js`, `src/cli.js` |
| Prompt | `src/prompt.js` |

## Known Limitations

Plans are generated by the active model and are not independently validated
against a formal task graph.

## Change History

| Date | Change | Reference |
|------|--------|-----------|
| 2026-08-30 | Registered plans preflight and progress behavior | `specs/2026-08-30-feature-registry-completion` |
