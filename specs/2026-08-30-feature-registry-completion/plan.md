# Plan: Complete the retroactive feature registry

1. Add registry entries for the agent loop, built-in tools, plans mode and
   context compression using the existing template and their implemented specs.
2. Add all four rows to `features/README.md`, update Phase 0 and quality
   deliverables in `docs/roadmap.md`, and close backlog item 8.4.
3. Add a changelog entry, verify relative links and Markdown consistency, then
   record evidence in `tasks.md`.
